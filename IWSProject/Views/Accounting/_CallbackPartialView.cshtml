@Html.DevExpress().CallbackPanel(settings =>
{
    settings.Name = "callbackPartialView";
    settings.CallbackRouteValues = new { Controller = "Accounting", Action = "CallbackPanelPartial" };

    settings.SetContent(() =>
    {
        @Html.RenderPartial("ValidateBLPartialView");

    });
    settings.ClientSideEvents.BeginCallback = "OnBeginCallback";
}).GetHtml()

